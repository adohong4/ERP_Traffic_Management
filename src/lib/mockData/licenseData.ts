import type { DriverLicense } from '@/types';

// export const getLicenseById = (id: string): DriverLicense | undefined => {
//   return mockLicenses.find((license) => license.id === id);
// };

// export const getLicenseByNumber = (licenseNumber: string): DriverLicense | undefined => {
//   return mockLicenses.find((license) => license.licenseNumber === licenseNumber);
// };

// export const getLicensesByStatus = (status: string): DriverLicense[] => {
//   return mockLicenses.filter((license) => license.status === status);
// };

// export const getApplicationById = (id: string): LicenseApplication | undefined => {
//   return mockLicenseApplications.find((app) => app.id === id);
// };