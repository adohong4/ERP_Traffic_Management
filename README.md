
  # ERP for Driver License Management

  This is a code bundle for ERP for Driver License Management.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  